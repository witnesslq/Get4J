<?xml version="1.0" encoding="UTF-8"?>

<!-- 定义爬虫系统全局设置 -->
<configuration>

    <property>
        <name>download.file.name.rule</name>
        <value>default</value>
        <description>
            非必填项。下载的文件名命名规则，有两种模式：url/default，url表示文件名带有具体网络地址，默认值default表示无url前缀，如果选择url的话，
			url中的特殊字符?|等会被下划线_替换，否则在操作系统中生成文件名会报错。default模式下如果抓取的是动态网站，HttpClient无法获取到首页名称的话，
            那么它会按照index.html自动补全
        </description>
    </property>

</configuration>
